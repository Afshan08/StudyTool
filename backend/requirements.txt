# Core Django
Django>=4.2
djangorestframework>=3.14.0
django-cors-headers>=4.0.0

# Production static file serving (no separate web server needed)
whitenoise>=6.6.0

# Production WSGI server (Windows-compatible, pure Python)
waitress>=3.0.0

# PostgreSQL adapter
psycopg2-binary>=2.9.9

# Environment variable management
python-decouple>=3.8

# AI & Speech-to-Text Pipeline
openai>=1.0.0
faster-whisper>=1.0.0

